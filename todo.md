TODO: I looking for a strategy for backup the services, some of them have a "/task/data-backup.sh" and "/task/data-restore.sh" scripts, also if you look at the docker-compose files i normalized that every services look for backups on its own folder inside "./data/backup", now not all services has the scripts, also they differ on databases engines, i will want to know if theres a way to have a root level folder "data/backup", gitignored in which can have listed in folders the name of the service and inside all the backups files for each one; i copied an old folder that i have on that exact location "data/backup" and it has the structure that i want, also have real backups that i took before, but i put these manually there i want an automated process that do this for me; maybe the will be complications because of the scoping the container have to access outside files because these backups are generated inside the containers themselves the can only see the "./data/backup" in the services folder for each one, so maybe we keep this method and then for the root level folder "/data/backup" the services folder are symlinks to the "/serv/<service-name>/data/backup" one, please review and let me know best aproach on this; also later i will need to generate scripts for all the missing services inside their task folders, but we can work this on steps for now analize the strategy
