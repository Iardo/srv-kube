# NOTE:
# Used only by Komodo's own stack deploy (see komodo-stk.toml).
include: [
  '${SERV:?}/actual/${FILE:?}',
  '${SERV:?}/caddy/${FILE:?}',
  '${SERV:?}/dnsmasq/${FILE:?}',
  '${SERV:?}/linkace/${FILE:?}',
  '${SERV:?}/ntfy/${FILE:?}',
  # astralapp
  # caddy
  # silverbullet
]
