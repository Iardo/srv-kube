# Instructions
# --------------------
# docker compose up -d --build

include: [
  '${SERV:?}/open-project/${FILE:?}',
  '${SERV:?}/portainer/${FILE:?}',
  '${SERV:?}/scrutiny/${FILE:?}',
  '${SERV:?}/speedtest-tracker/${FILE:?}',
]
