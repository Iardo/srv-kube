# NOTE:
# Used only by Komodo's own stack deploy (see komodo-stk.toml).
include: [
  '${SERV:?}/authentik/${FILE:?}',
  '${SERV:?}/caddy/${FILE:?}',
  '${SERV:?}/dnsmasq/${FILE:?}',
  '${SERV:?}/open-project/${FILE:?}',
  '${SERV:?}/outline/${FILE:?}',
  # beszel
  # bugsink
  # caddy
  # coolify
  # dagger
  # gatus
  # grafana
  # infisical (optional)
  # mailpit
  # passbolt
  # umami
  # webcheck
  # woodpecker
]
