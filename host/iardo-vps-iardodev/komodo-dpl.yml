# NOTE:
# Used only by Komodo's own stack deploy (see komodo-stk.toml).
include: [
  '${SERV:?}/authentik/${FILE:?}',
  '${SERV:?}/open-project/${FILE:?}',
  '${SERV:?}/outline/${FILE:?}',
  # beszel
  # bugsink
  # caddy
  # dagger
  # gatus
  # mailpit
  # passbolt
  # signoz
  # umami
  # woodpecker
]
